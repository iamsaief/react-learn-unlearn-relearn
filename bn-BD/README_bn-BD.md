<div align="center">
  <img height="80" src="https://svgmix.com/uploads/e86a0a-react.svg">
  <h1>রিএক্টঃ শিখুন-ভুলুন-আবার শিখুন ⚛️</h1>
</div>

আপনার **রিএক্ট দক্ষতা পরীক্ষা করুন**, গুরুত্বপূর্ণ ধারণাগুলি পুনঃজীবিত করুন, অথবা **ইন্টারভিউয়ের জন্য প্রস্তুতি নিন!** 💪 এই **সম্পূর্ণ ডকুমেন্ট গাইড** আপনাকে **রিএক্ট-এর গভীর বিষয়গুলি পরিষ্কারভাবে শেখাতে** সাহায্য করবে, যেখানে রয়েছে **বিশদ ব্যাখ্যা, একাধিক উদাহরণ, সাধারণ ভুলত্রুটি এবং বাস্তব জীবনের প্রয়োগ**।

আমি এটি নিজের **শিখন ও অবসরে আনন্দের জন্য তৈরি করেছি**, তাই **যদি কোনো ভুল থেকে থাকে, দয়া করে ক্ষমা করবেন**। 😊 আপনি যদি এটি **উপকারী মনে করেন**, তাহলে **⭐️ দিয়ে সাহায্য করুন বা রেফারেন্স দিন**, এটি আমাকে আরও ভালো করতে অনুপ্রেরণা দেবে!

আপনার **প্রোগ্রামিং যাত্রায় শুভকামনা—** 🙏✨ হ্যাপি কোডিং! 🧑‍💻

<p align="center">
💬 যদি আমার সাথে যোগাযোগ করতে চান, ↩️ <br/>
<a href="https://www.facebook.com/saiefalemon">Facebook</a> | <a href="https://www.linkedin.com/in/saiefalemon">LinkedIn</a> | <a href="https://www.iamsaief.com/">Blog</a>

---

**🗂️ সূচিপত্র:**

- [১. ⚡ `useEffect()` মাস্টারি করা – React-এর সাইড এফেক্ট পাওয়ারহাউস](#১--useeffect-মাস্টারি-করা--react-এর-সাইড-এফেক্ট-পাওয়ারহাউস)
- [২. 📜 `useState()` ব্যাখ্যা – কম্পোনেন্ট স্টেট পরিচালনা করার মাস্টারক্লাস](#২--usestate-ব্যাখ্যা--কম্পোনেন্ট-স্টেট-পরিচালনা-করার-মাস্টারক্লাস)
- [৩. 🔍 `useRef()`-এর দক্ষ ব্যবহার – রি-রেন্ডার ছাড়াই সরাসরি DOM-এ অ্যাক্সেস](#৩--useref-এর-দক্ষ-ব্যবহার--রি-রেন্ডার-ছাড়াই-সরাসরি-dom-এ-অ্যাক্সেস)
- [৪. 🌍 React Context API – Prop Drilling সমস্যার সমাধান](#৪--react-context-api--prop-drilling-সমস্যার-সমাধান)
- [৫. 🚀 `React.memo()` এবং `useMemo()` ব্যবহার করে React পারফরম্যান্স উন্নত করা](#৫--reactmemo-এবং-usememo-ব্যবহার-করে-react-পারফরম্যান্স-উন্নত-করা)

---

## ১. ⚡ `useEffect()` মাস্টারি করা – React-এর সাইড এফেক্ট পাওয়ারহাউস

**🔹 ভূমিকা**

React-এর `useEffect()` হুকটি **সাইড এফেক্ট পরিচালনা** করতে সাহায্য করে ফাংশনাল কম্পোনেন্টে—যেমন **ডাটা ফেচিং, ইভেন্ট লিসেনার, সাবস্ক্রিপশন, বা DOM আপডেট** যা React-এর রেন্ডারিং সাইকেলের বাইরে ঘটে। `useEffect()` ছাড়াই আপনাকে **ম্যানুয়ালি লাইফসাইকেল মেথডগুলো (`componentDidMount()`, `componentDidUpdate()`) ব্যবহার করে সাইড এফেক্ট ম্যানেজ করতে হতো**।

### 💡 সহজ উদাহরণ: রান্না করার সময় টাইমার ব্যবহার

ধরো তুমি রান্না করার সময় পানি ফুটিয়ে নিচ্ছো। তুমি একটি **টাইমার সেট করো যেন পানি ঠিক সময়ে ফুটে**। একইভাবে, `useEffect()` নির্দিষ্ট অ্যাকশন **কম্পোনেন্ট রেন্ডার হওয়ার পর চালায়**, নিশ্চিত করে **সেগুলো প্রয়োজন অনুযায়ী এক্সিকিউট হয়**।

### 📝 উদাহরণ ১: কম্পোনেন্ট মাউন্ট হলে API ডাটা ফেচ করা

```jsx
import { useEffect, useState } from "react";

const FetchComponent = () => {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch("https://api.example.com/data")
      .then((response) => response.json())
      .then((json) => setData(json));
  }, []);

  return <div>{data ? JSON.stringify(data) : "লোড হচ্ছে..."}</div>;
};
```

**💡 ব্যাখ্যা:**

- ফাঁকা ডিপেন্ডেন্সি অ্যারে (`[]`) নিশ্চিত করে এই ইফেক্টটি **শুধু একবার চলবে**, `componentDidMount()`-এর মতো।

- `useEffect()` ছাড়া API কল **প্রত্যেকবার রেন্ডারে চলবে**, যা **পারফরম্যান্স সমস্যার কারণ হতে পারে**।

### 📝 উদাহরণ ২: ইভেন্ট লিসেনার ক্লিনআপ করা

```jsx
import { useEffect } from "react";

const ResizeComponent = () => {
  useEffect(() => {
    const handleResize = () => console.log("উইন্ডো রিসাইজ হয়েছে!");

    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize); // ক্লিনআপ
  }, []);

  return <div>উইন্ডো রিসাইজ করে ইফেক্ট দেখুন।</div>;
};
```

**💡 ব্যাখ্যা:**

- **ক্লিনআপ ফাংশন** ইভেন্ট লিসেনার **কম্পোনেন্ট আনমাউন্ট হলে সরিয়ে ফেলে**, যা **মেমোরি লিক প্রতিরোধ করে**।

**📌 সাধারণ ভুল**
ইভেন্ট লিসেনার **রিমুভ করতে ভুলে যাওয়া**, যার ফলে **অনর্থক লিসেনার জমতে থাকে**।

### 📌 বাস্তব জীবনে কোথায় দেখতে পাবেন

- **কম্পোনেন্ট লোড হলে API ডাটা ফেচ করা**
- **অ্যানিমেশনের জন্য ইন্টারভাল সেট করা**
- **WebSocket সংযোগ পরিচালনা করা**

---

## ২. 📜 `useState()` ব্যাখ্যা – কম্পোনেন্ট স্টেট পরিচালনা করার মাস্টারক্লাস

**🔹 ভূমিকা**

State React কম্পোনেন্টগুলোকে **ডাইনামিক ডাটা ট্র্যাক করতে** এবং **ডাটা পরিবর্তিত হলে পুনরায় রেন্ডার করতে** সাহায্য করে। `useState()` হলো **React-এর সবচেয়ে মৌলিক Hook**, যা ক্লাস কম্পোনেন্টে `this.state`-এর বিকল্প হিসেবে কাজ করে।

### 💡 সহজ উদাহরণ: তাৎক্ষণিক নোটের জন্য নোটপ্যাড ব্যবহার

`useState()`-কে চিন্তা করুন **স্টিকি নোটের** মতো—তুমি সেখানে তথ্য লেখো, **যেকোনো সময়ে আপডেট করতে পারো**, এবং **প্রয়োজনমতো দেখতে পারো**। সাধারণ ভেরিয়েবলের মতো নয়, React **রেন্ডারের মধ্যেও `useState()`-এর মান মনে রাখে**।

### 📝 উদাহরণ ১: একটি সাধারণ কাউন্টার তৈরি করা

```jsx
import { useState } from "react";

const Counter = () => {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
};
```

**💡 ব্যাখ্যা:**

- `useState(0)` **স্টেটকে ০ দিয়ে ইনিশিয়ালাইজ করে**।
- `setCount(count + 1)` **স্টেট আপডেট করে** এবং **পুনরায় রেন্ডার ট্রিগার করে**।

**📌 সাধারণ ভুল:** **স্টেট সরাসরি পরিবর্তন করা** (`count = 5`), যা **রেন্ডার ট্রিগার করবে না**।

### 📝 উদাহরণ ২: অ্যাসিনক্রোনাস স্টেট আপডেট পরিচালনা করা

```jsx
import { useState, useEffect } from "react";

const MessageComponent = () => {
  const [message, setMessage] = useState("");

  useEffect(() => {
    setTimeout(() => setMessage("ডাটা লোড হয়েছে!"), 2000);
  }, []);

  return <p>{message || "লোড হচ্ছে..."}</p>;
};
```

**💡 ব্যাখ্যা:**

- **স্টেট অ্যাসিনক্রোনাসভাবে আপডেট হয় ২ সেকেন্ড পরে**, যা বাস্তব API রেসপন্সের মতো কাজ করে।

### 📌 বাস্তব জীবনে কোথায় ব্যবহার হয়

- **ব্যবহারকারীর ইনপুট পরিচালনা করতে** (সার্চ বার, ফর্ম)
- **UI স্টেট পরিচালনা করতে** (মডাল, নোটিফিকেশন)
- **API রেসপন্স সংরক্ষণ করতে**

## ৩. 🔍 `useRef()`-এর দক্ষ ব্যবহার – রি-রেন্ডার ছাড়াই সরাসরি DOM-এ অ্যাক্সেস

**🔹 ভূমিকা**

React-এর `useRef()` **রেন্ডারগুলোর মধ্যে মান সংরক্ষণ করে** কিন্তু **রি-রেন্ডার ট্রিগার করে না**। `useState()`-এর মতো নয়, `useRef()`-এর মান পরিবর্তিত হলেও **UI পুনরায় রেন্ডার হয় না**, যা এই ক্ষেত্রে কার্যকর:

- **DOM এলিমেন্টে সরাসরি অ্যাক্সেস ও নিয়ন্ত্রণ** (যেমন ইনপুট ফোকাস)
- **পূর্ববর্তী মান ট্র্যাক করা পারফরম্যান্স প্রভাব ছাড়াই**
- **ব্যয়বহুল গণনার অপ্টিমাইজেশন**

`useRef()` ছাড়া তোমাকে **স্টেট ব্যবহার করে মান সংরক্ষণ করতে হতে পারে**, যা **অনর্থক রি-রেন্ডার সৃষ্টি করতে পারে**।

### 💡 সহজ উদাহরণ: বইয়ের মধ্যে বুকমার্ক রাখা

কল্পনা করো তুমি একটি **বইয়ে বুকমার্ক রেখে দিচ্ছো** যাতে প্রয়োজনমতো সেই পৃষ্ঠায় ফিরতে পারো। বুকমার্ক **একই জায়গায় থাকে**, তুমি বই যতবারই পড়ো না কেন। একইভাবে, `useRef()` **মান সংরক্ষণ করে রি-রেন্ডার ট্রিগার না করে**, যা `useState()`-এর থেকে ভিন্ন।

### 📝 উদাহরণ ১: ইনপুট ফিল্ডকে প্রোগ্রাম্যাটিকভাবে ফোকাস করা

```jsx
import { useRef } from "react";

const InputFocus = () => {
  const inputRef = useRef(null);

  return (
    <div>
      <input ref={inputRef} />
      <button onClick={() => inputRef.current.focus()}>ফোকাস ইনপুট</button>
    </div>
  );
};
```

**💡 ব্যাখ্যা:**

- `useRef(null)` **একটি ইনপুট এলিমেন্টের রেফারেন্স ইনিশিয়ালাইজ করে**।
- বাটনে ক্লিক করলে `inputRef.current.focus()` **কল হয়**, যা সরাসরি DOM-এর সাথে যোগাযোগ করে।

`useState()`-এর বিপরীতে, `useRef()` **পরিবর্তিত হলেও রেন্ডার ট্রিগার হয় না**, যা পারফরম্যান্স ভালো রাখে।

**📌 সাধারণ ভুল:** `useRef()`-এর মান পরিবর্তিত হলেও **রেন্ডার হওয়ার আশা করা—এটি রেন্ডার ট্রিগার করে না**।

### 📝 উদাহরণ ২: পূর্ববর্তী স্টেট ট্র্যাক করা রেন্ডারিং প্রভাব ছাড়াই

```jsx
import { useRef, useEffect, useState } from "react";

const PrevValue = () => {
  const [count, setCount] = useState(0);
  const prevCountRef = useRef(count);

  useEffect(() => {
    prevCountRef.current = count;
  }, [count]);

  return (
    <div>
      <p>বর্তমান কাউন্ট: {count}</p>
      <p>পূর্ববর্তী কাউন্ট: {prevCountRef.current}</p>
      <button onClick={() => setCount(count + 1)}>বাড়াও</button>
    </div>
  );
};
```

**💡 ব্যাখ্যা:**

- `prevCountRef.current` পূর্ববর্তী `count` **সংরক্ষণ করে অতিরিক্ত রেন্ডার ছাড়াই**।
- `count` আপডেট হলে, **ইফেক্ট `prevCountRef.current` আপডেট করে**, পূর্ববর্তী মান সংরক্ষণ করে।
- `useState()`-এর মতো নয়, `useRef()` পরিবর্তিত হলেও **রি-রেন্ডার হয় না**।

### 📌 বাস্তব জীবনে কোথায় ব্যবহার হয়

- **অ্যানকন্ট্রোলড ফর্ম ইনপুট পরিচালনা করতে**
- **অ্যানিমেশন বা গণনার জন্য পূর্ববর্তী মান ট্র্যাক করতে**
- **Mutable ভ্যালু সংরক্ষণ করতে যা রেন্ডার ট্রিগার করা উচিত নয়**

---

## ৪. 🌍 React Context API – Prop Drilling সমস্যার সমাধান

**🔹 ভূমিকা**

React Context API **prop drilling সমস্যার সমাধান করে**, যেখানে ডা**টা একাধিক লেয়ার গভীরে পাঠাতে হয়** props ব্যবহার করে। একাধিক কম্পোনেন্টের মধ্য দিয়ে props পাঠানোর পরিবর্তে, Context API **একটি গ্লোবাল স্টেট প্রদান করে, যা যেকোনো কম্পোনেন্ট ব্যবহার করতে পারে**।

### 💡 সহজ উদাহরণ: একটি পাবলিক বুলেটিন বোর্ড

একটি বিল্ডিংয়ের **প্রত্যেক ব্যক্তিকে আলাদাভাবে বার্তা পাঠানোর পরিবর্তে**, একটি **বুলেটিন বোর্ড** সবাইকে তথ্য শেয়ার করতে সাহায্য করে। একইভাবে, React Context API **ডাটাকে গ্লোবালি শেয়ার করে**, যা **গভীর props চেইন এড়াতে সাহায্য করে**।

### 📝 উদাহরণ ১: একটি থিম কনটেক্সট তৈরি করা

```jsx
import { createContext } from "react";

const ThemeContext = createContext("light");

const App = () => (
  <ThemeContext.Provider value="dark">
    <ChildComponent />
  </ThemeContext.Provider>
);
```

**💡 ব্যাখ্যা:**

- `createContext()` **একটি গ্লোবাল থিম ইনিশিয়ালাইজ করে**।

- `Provider` **চাইল্ড কম্পোনেন্টগুলোকে ঘিরে রাখে**, যা তাদের **থিম ভ্যালু (dark) অ্যাক্সেস করতে দেয়**।

**📌 সাধারণ ভুল**: কম্পোনেন্টগুলোকে **Provider দিয়ে না মোড়ানো**, যার ফলে `undefined` **কনটেক্সট মান পাওয়া যেতে পারে**।

### 📝 উদাহরণ ২: কনটেক্সট কম্পোনেন্টে ব্যবহার করা

```jsx
import { useContext } from "react";

const theme = useContext(ThemeContext);
return <div style={{ backgroundColor: theme === "dark" ? "#333" : "#fff" }}>থিম: {theme}</div>;
```

**💡 ব্যাখ্যা:**

- `useContext(ThemeContext)` নিকটবর্তী প্রদত্ত মান নেয়, যা **ম্যানুয়ালি props পাঠানো থেকে মুক্তি দেয়**।

### 📌 বাস্তব জীবনে কোথায় ব্যবহার হয়

- **Authentication state পরিচালনা**
- **থিম পরিবর্তন ও ইউজার প্রেফারেন্স**
- **মাল্টি-ল্যাঙ্গুয়েজ সাপোর্ট ব্যবস্থাপনা**

---

## ৫. 🚀 `React.memo()` এবং `useMemo()` ব্যবহার করে React পারফরম্যান্স উন্নত করা

**🔹 ভূমিকা**

React-এ স্টেট বা **props পরিবর্তিত হলে কম্পোনেন্ট পুনরায় রেন্ডার করে**, কিন্তু কখনো কখনো **এই রি-রেন্ডারিং অপ্রয়োজনীয় হয়ে** যায়। `React.memo()` এবং `useMemo()`**অপ্রয়োজনীয় পুনঃগণনা এড়িয়ে পারফরম্যান্স অপ্টিমাইজ করে**।

### 💡 সহজ উদাহরণ: পুনরায় নোট লেখার পরিবর্তে চিট শিট ব্যবহার

Memoization **পূর্বে গণনা করা মান সংরক্ষণ করে**, যাতে **পুনরায় একই হিসাব করতে না হয়**। এটি **চিট শিটে উত্তর লিখে রাখা**-এর মতো, যেখানে **প্রতিবার নতুন করে সমস্যা সমাধান করার দরকার হয় না**।

### 📝 উদাহরণ ১: `React.memo()` ব্যবহার করে অপ্রয়োজনীয় রি-রেন্ডার প্রতিরোধ

```jsx
import React from "react";

const MemoizedComponent = React.memo(({ count }) => <div>Count: {count}</div>);
```

**💡 ব্যাখ্যা:**

- `React.memo()` নিশ্চিত করে **এই কম্পোনেন্ট শুধুমাত্র `count` পরিবর্তিত হলে রেন্ডার হয়**, যা **অপ্রয়োজনীয় আপডেট কমায়**।

**📌 সাধারণ ভুল:** `React.memo()` **পরিবর্তিত চাইল্ড এলিমেন্টের ক্ষেত্রে ব্যবহার করা**, যা **অপ্রত্যাশিত রেন্ডারিং সমস্যা তৈরি করতে পারে**।

### 📝 উদাহরণ ২: `useMemo()` ব্যবহার করে ব্যয়বহুল গণনা অপ্টিমাইজ করা

```jsx
import { useMemo, useState } from "react";

const ExpensiveCalculation = ({ num }) => {
  const squaredNumber = useMemo(() => num * num, [num]);

  return <p>Squared: {squaredNumber}</p>;
};
```

**💡 ব্যাখ্যা:**

- `useMemo()` **গণিত ফাংশন ক্যাশ করে**, যা **প্রত্যেকবার পুনরায় গণনা করতে দেয় না**।

### 📌 বাস্তব জীবনে কোথায় ব্যবহার হয়

- **বড় তালিকা বা জটিল গণনা অপ্টিমাইজ করতে**
- **অপ্রয়োজনীয় কম্পোনেন্ট রি-রেন্ডার প্রতিরোধ করতে**
- **UI-ভারী অ্যাপে ল্যাগ কমাতে**

---
